# 1 – Basic Make Generator

Demonstrates the smallest possible CMake project:

- single executable built from `src/main.cpp`
- explicit C++17 standard setting for deterministic builds
- Makefiles generated via `cmake -G "Unix Makefiles"`

```sh
./build.sh
```

